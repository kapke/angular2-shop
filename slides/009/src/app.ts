//5/ We need to bootstrap application manually by calling `bootstrap` function with main component class
import {bootstrap} from '@angular/platform-browser-dynamic';
import App from './components/App/App';

bootstrap(App);
