extends ../../template/slide

block title
    | Custom events

block layout
    - var index = '02';
    .xp-column
        .xp-row: xp-editor
            +includeFile(index, 'app.ts', true)
            +includeFile('', 'index.html')
            +includeFile('', 'config.js')
        .xp-row: xp-editor
            +includeFile(index, 'Timer.ts', true)
    .xp-column
        +previewTs
    +invisibleEditor('', [
        'node_modules/angular2/bootstrap.js'
    ])
