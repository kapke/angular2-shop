extends ../../template/slide

block title
    | The simplest example of DI usage

block layout
    - var index = '01';
    .xp-column: xp-editor
        +includeFile(index, 'app.ts', true)
        +includeFile('', 'Todo.ts')
        +includeFile('', 'index.html')
        +includeFile('', 'config.js')
    .xp-column
        .xp-row
            +previewTs
    +invisibleEditor('', [
        'node_modules/angular2/bootstrap.js'
    ])
