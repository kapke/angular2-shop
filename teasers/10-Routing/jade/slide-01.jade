extends ../../template/slide

block title
    | Simple Routing

block layout
    - var index = '01';
    .xp-column
        .xp-row: xp-editor
            +includeFile('', 'index.html', true)
        .xp-row: xp-editor
            +includeFile(index, 'app.ts', true)
            +includeFile('', 'config.js')
    .xp-column
        .xp-row
            +previewFakeTs
        .xp-row: xp-editor
            +includeFile(index, 'app.components.ts', true)
