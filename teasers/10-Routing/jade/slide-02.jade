extends ../../template/slide

block title
    | Nested routing

block layout
    - var index = '02';
    .xp-column: xp-editor
        +includeFile(index, 'app.ts', true)
        +includeFile('', 'index.html')
        +includeFile('', 'config.js')
    .xp-column
        .xp-row: +previewTs
        .xp-row: xp-editor
            +includeFile(index, 'Todo.ts', true)
    +invisibleEditor('', [
        'node_modules/angular2/bootstrap.js'
    ])
